import area_01
import area_01 as ar
from area_01 import circle, square

print(dir((circle)))  # dir 함수는 파일의 모든 namespace를 보여준다.

# double underscore(DUNDER)가 있는 것은 특수 변수

# import 방식에 따라 파일에 추가되는 방식이 달라진다.